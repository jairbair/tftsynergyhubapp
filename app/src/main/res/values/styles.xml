<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="ButtonDefault" parent="AppTheme">
        <item name="android:textColor">@color/gold</item>
        <item name="android:shadowColor">#121212</item>
        <item name="android:shadowDx">0</item>
        <item name="android:shadowDy">0</item>
        <item name="android:shadowRadius">2</item>
        <item name="android:background">@drawable/button_default_shape</item>
    </style>
    <style name="ButtonSelected" parent="AppTheme">
        <item name="android:textColor">@color/ic_launcher_background</item>
        <item name="android:background">@drawable/button_selected_shape</item>
    </style>
</resources>
